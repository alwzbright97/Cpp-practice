#include <iostream>

int N, stat[20][20], player[20];
int start = 0, link = 0;
void dfs()
{
    if (/* condition */)
    {
        /* code */
    }
}
int main()
{
    std::cout.tie(NULL);
    std::ios::sync_with_stdio(false);
}